推薦 Tech Stack

### 1. MVP 技術清單

| 功能                | 推薦技術                      | 理由                              |
| ----------------- | ------------------------- | ------------------------------- |
| 程式語言              | **Python 3.11/3.12**      | ML 生態成熟、跨平台                     |
| ASR               | **Qwen3-ASR 0.6B / 1.7B** | 固定需求                            |
| ASR Runtime       | **Qwen 官方推薦 runtime 優先**  | 避免自行猜測最佳推論方式                    |
| 錄音                | **sounddevice**           | 跨 macOS / Windows、API 簡單、適合 MVP |
| 音訊處理              | **NumPy**                 | sounddevice 與 ASR 音訊資料銜接方便      |
| 全域熱鍵              | **keyboard**              | 你已指定                            |
| 繁中轉換              | **OpenCC**                | 成熟、離線、適合繁簡轉換                    |
| Custom Dictionary | **Python 標準 JSON**        | 不需要額外 dependency                |
| Clipboard         | **pyperclip**             | 非常簡單、跨平台                        |
| Paste             | **keyboard**              | 已經使用 keyboard，可減少 dependency    |
| Menu Bar / Tray   | **pystray**               | 輕量、macOS / Windows 都能用          |
| 設定                | **JSON**                  | 與 Custom Dictionary 一致、容易人工修改   |
| Logging           | **Python logging**        | 標準庫即可                           |
| Testing           | **pytest**                | Python 生態標準選擇                   |
| Type checking     | **mypy 或 pyright**        | 建議至少選一個                         |
| Packaging         | **PyInstaller**           | MVP 最簡單的跨平台打包方案                 |

---

## 2. 我特別推薦這個組合

如果現在直接開始 Coding，我會先用：

```text
Python
├── Qwen3-ASR
├── sounddevice
├── numpy
├── keyboard
├── OpenCC
├── pyperclip
├── pystray
└── PyInstaller
```

其他部分全部盡量使用 Python Standard Library。

也就是：

```text
JSON          → stdlib
Config        → stdlib
Logging       → stdlib
File System   → pathlib
Threading     → threading
Process       → subprocess
```

這樣 dependency 數量很少。

---

# 3. 錄音：sounddevice 我會保留

你之前提到 `sounddevice`，我認為它其實是很好的選擇。

原因：

* macOS / Windows 都支援
* API 很簡單
* 不需要碰太多 OS audio API
* 可以直接取得 NumPy audio array
* 適合「按 F8 → 錄音 → 按 F8 → 結束」這種模式

因此：

> **sounddevice 可以從「候選技術」提升成 MVP 推薦技術。**

---

# 4. Clipboard：pyperclip

這個我也推薦保留。

流程：

```text
Final Text
    ↓
pyperclip.copy(text)
    ↓
Paste
```

它本身非常簡單，不需要為 Clipboard 建立複雜 abstraction。

不過架構上仍然建議包一層：

```python
ClipboardService
```

未來如果 macOS / Windows 行為不同，可以替換 implementation。

---

# 5. Paste：我會稍微保留彈性

你原本的：

> `pyperclip` + 模擬貼上快捷鍵

方向沒問題。

第一候選：

```text
keyboard
```

因為你本來就需要：

```text
keyboard
    ├── F8 Global Hotkey
    └── Cmd/Ctrl + V
```

這可以少一個 dependency。

但這裡我會要求 Coding LLM **實際驗證 macOS 與 Windows**。

尤其 macOS 的：

```text
Command + V
```

涉及 Accessibility / Input Monitoring 權限。

如果 `keyboard` 在 macOS 的 Paste 模擬不夠穩，再針對 macOS 使用平台專用方案，而不是為了理論上的跨平台而犧牲穩定性。

---

# 6. Menu Bar / System Tray：pystray

我會推薦：

**pystray**

原因是你的需求非常小：

```text
Voice Input
├── Model
│   ├── ✓ 0.6B
│   └── 1.7B
├── Open Dictionary
├── Status
└── Quit
```

根本不需要 Qt / Tkinter 這種完整 GUI framework。

因此：

> **不要為了 Menu Bar 而引入 PyQt / PySide。**

這會違反你「極簡」的產品定位。

---

# 7. Config：不要引入額外套件

直接：

```text
config.json
```

例如：

```json
{
  "model": "0.6b"
}
```

Python：

```text
json
pathlib
```

就夠了。

---

# 8. Custom Dictionary

同樣直接：

```text
custom_dictionary.json
```

例如：

```json
{
  "version": 1,
  "entries": [
    {
      "target": "程式",
      "variants": ["城市"]
    },
    {
      "target": "介面",
      "variants": ["接口"]
    }
  ]
}
```

不需要 SQLite。

不需要資料庫。

不需要 ORM。

---

# 9. Async / Threading

這個工具有一個很重要的 UX 要求：

**ASR 推論不能阻塞 F8 / UI。**

例如：

```text
Main Thread
│
├── Hotkey
├── Tray
└── State
      │
      └──── Worker Thread
                │
                └── ASR inference
```

我會優先使用：

```python
threading
```

而不是一開始就引入：

* asyncio
* Celery
* multiprocessing framework

你的工作流本質上是：

```text
錄音
→
一次 inference
→
完成
```

不需要複雜 concurrency framework。

---

# 10. Testing

推薦：

**pytest**

測試重點不是測 Qwen 模型本身，而是測我們自己的 pipeline。

例如：

```text
test_dictionary.py
```

測：

```text
城市 → 程式
接口 → 介面
```

以及：

```text
test_text_pipeline.py
```

測：

```text
ASR result
 ↓
OpenCC
 ↓
Dictionary
 ↓
Final Text
```

另外測：

```text
test_config.py
test_state.py
test_model_manager.py
```

ASR 真實 inference 測試可以另外分類成 integration test。

---

# 11. Type checking

我推薦：

**Pyright**

理由是：

* VS Code 支援很好
* 對 Python type hint 支援成熟
* 對 Coding LLM 產生的 Python code 也很有幫助

如果你希望 dependency 再少一點，也可以用 `mypy`。

但兩個**不要同時上**。

---

# 12. Packaging

MVP 我推薦：

**PyInstaller**

因為目標是：

```text
Python project
        ↓
PyInstaller
        ↓
macOS app
Windows exe
```

朋友不需要：

```text
Python
pip
venv
```

### 但有一個重要提醒

Qwen3-ASR 模型本身可能非常大。

因此：

**「Python 程式打包成 exe/app」和「ASR 模型如何分發」應該分開設計。**

不要把幾 GB 的模型硬塞進 executable。

比較合理：

```text
VoiceInput
├── Application
├── Runtime dependencies
├── Models
│   ├── Qwen3-ASR-0.6B
│   └── Qwen3-ASR-1.7B
└── custom_dictionary.json
```

或者第一次啟動時下載模型。

具體採哪一種，要看 Qwen3-ASR 官方模型載入方式與模型大小再決定。

---

# 13. 我會避免的技術

目前我**不推薦**：

### PyQt / PySide

太重。

你的 UI 只有：

* Menu Bar
* Tray
* Menu

沒有必要。

### Electron

完全不符合這個工具的產品定位。

### SQLite

詞庫目前幾十、幾百甚至幾千筆都不需要 DB。

### Redis / Server

完全不需要。

### FastAPI

這是 local application，不是 web service。

### Docker

朋友使用端沒有價值。

### asyncio

目前沒有足夠複雜的 async workflow。

### Local Web UI

沒有必要。

---

# 14. 最終推薦 Stack

如果要我直接替你做技術決策，我會給 Coding LLM：

```text
Language
Python 3.11+

ASR
Qwen3-ASR-0.6B
Qwen3-ASR-1.7B

Audio
sounddevice
numpy

Global Hotkey
keyboard

Traditional Chinese
OpenCC

Clipboard
pyperclip

Paste
keyboard
└─ 若 macOS 實測不穩，再加入平台專用 implementation

Menu Bar / System Tray
pystray

Configuration
JSON + pathlib

Concurrency
threading

Logging
logging

Testing
pytest

Type Checking
pyright

Packaging
PyInstaller
```

### Dependency 原則

```text
Qwen3-ASR
sounddevice
numpy
keyboard
OpenCC
pyperclip
pystray
PyInstaller
pytest
pyright
```

其餘：

**Python Standard Library 優先。**

這套組合我認為很符合你的目標：**小、跨平台、本地、容易讓 Coding LLM 開發，也不會為了做一個 F8 語音輸入工具而引入一堆大型 framework。**
