開發規格
# 本地端繁體中文 AI 語音打字工具

## MVP 開發規格 v0.2

---

## 1. 專案目標

開發一個：

> **極簡、本地端、跨平台、以繁體中文為優先的 AI 語音打字工具。**

主要使用情境：

1. 使用者啟動程式。
2. 程式常駐背景。
3. 使用者在任何可以輸入文字的應用程式中按下 `F8`。
4. 開始錄音。
5. 使用者再次按下 `F8`。
6. 停止錄音。
7. 使用本地 Qwen3-ASR 模型進行語音辨識。
8. 執行繁體中文處理與 Custom Dictionary 校正。
9. 將最終文字放入 Clipboard。
10. 模擬系統貼上快捷鍵。
11. 文字直接輸入目前游標所在的位置。

核心體驗：

```text
F8
 ↓
開始錄音
 ↓
說話
 ↓
F8
 ↓
停止錄音
 ↓
ASR
 ↓
繁體中文後處理
 ↓
Custom Dictionary
 ↓
Clipboard
 ↓
Paste
 ↓
文字出現在目前游標位置
```

---

# 2. 設計原則

## 2.1 極簡

本工具不是：

* 錄音軟體
* 語音助理
* AI Chat
* 文字編輯器
* 語音工作站
* 雲端語音服務

只專注：

> **語音 → 繁體中文文字 → 輸入目前游標**

---

## 2.2 Local First

所有核心語音辨識均在本機執行。

MVP 不依賴：

* 雲端 ASR API
* OpenAI API
* Google Speech API
* 其他遠端語音服務

使用者的錄音內容不應為了 ASR 而上傳到第三方服務。

---

## 2.3 Traditional Chinese First

繁體中文是第一優先語言。

產品設計不得以簡體中文為主要中文標準。

優先考慮：

* 台灣繁體中文
* 台灣常用詞彙
* 繁體中文標點
* 使用者自訂詞彙

---

## 2.4 Cross Platform

目標平台：

* macOS
* Windows

開發環境：

* macOS

核心 Python 程式應盡可能保持跨平台。

平台特定功能應隔離在薄薄的 platform layer：

* Global Hotkey
* Clipboard
* Paste Simulation
* Menu Bar / System Tray
* OS Permission

不要讓 OS-specific code 污染核心 ASR / Text Processing 邏輯。

---

# 3. 固定技術決策

以下項目為產品層級固定需求。

## 3.1 Programming Language

**Python**

---

## 3.2 ASR

使用：

**Qwen3-ASR**

MVP 必須支援兩種模型：

* `Qwen3-ASR-0.6B`
* `Qwen3-ASR-1.7B`

模型必須能夠由使用者切換。

不得將 1.7B 寫死為唯一模型。

---

## 3.3 Global Hotkey

使用：

**Python `keyboard`**

固定快捷鍵：

**F8**

行為：

```text
F8 → Start Recording
F8 → Stop Recording
```

---

## 3.4 Custom Dictionary

使用：

**JSON**

作為使用者可編輯的詞庫。

---

# 4. 不固定的技術選擇

以下項目由 Coding LLM 根據：

* 跨平台相容性
* Qwen 官方支援
* 穩定性
* 效能
* 安裝／打包難度
* macOS / Windows 實測

自行選擇最佳方案。

包括：

* Audio Recording library
* Clipboard library
* Paste simulation library
* OpenCC Python implementation
* Qwen ASR inference runtime
* Model download / cache mechanism
* Packaging solution
* Menu Bar / System Tray framework

不要因為本文件早期討論曾提到某個套件，就視為該套件是強制要求。

---

# 5. 使用者操作流程

## 5.1 啟動

程式啟動後：

1. 初始化設定。
2. 載入 Custom Dictionary。
3. 初始化 Global Hotkey。
4. 初始化系統 Menu Bar / System Tray。
5. 根據目前設定準備 ASR Model。

---

## 5.2 開始錄音

使用者按：

`F8`

系統：

```text
Idle
 ↓
Recording
```

需要提供基本的錄音狀態提示。

---

## 5.3 停止錄音

再次按：

`F8`

系統：

```text
Recording
 ↓
Processing
```

停止錄音並開始 ASR。

---

## 5.4 ASR

使用目前選定的模型：

```text
Qwen3-ASR-0.6B
```

或：

```text
Qwen3-ASR-1.7B
```

進行本地推論。

---

# 6. 繁體中文處理 Pipeline

處理順序：

```text
Audio
 ↓
Qwen3-ASR
 ↓
Traditional Chinese Prompt / ASR Configuration
 ↓
OpenCC
 ↓
Custom Dictionary
 ↓
Final Text
```

---

## 6.1 ASR Prompt

推論時加入繁體中文導向 prompt。

目標：

* 優先輸出繁體中文。
* 優先使用台灣中文語境。
* 減少簡體中文輸出。
* 提高中文標點與文字品質。

Prompt 的實際內容由 Coding LLM 依 Qwen3-ASR 官方能力與實測結果決定。

不要在沒有測試的情況下假設某個 Prompt 一定最佳。

---

# 7. OpenCC

OpenCC 作為繁體中文的**保底轉換層**。

用途：

* 簡體 → 繁體
* 必要的中文詞彙轉換

例如：

```text
软件 → 軟體
视频 → 影片
接口 → 介面
服务器 → 伺服器
```

OpenCC 不是本產品的主要語意校正系統。

它只負責：

> 確保輸出至少符合繁體中文要求。

---

# 8. Custom Dictionary

Custom Dictionary 是本產品的重要核心功能。

## 8.1 核心概念

不是：

```text
錯字 → 正字
```

而是：

```text
Target
←
ASR Variants
```

也就是：

> 使用者指定「我真正想使用的詞」，以及 ASR 可能辨識成的近音／同音／異用詞。

---

# 9. Custom Dictionary 格式

MVP 使用：

`custom_dictionary.json`

格式：

```json
{
  "version": 1,
  "entries": [
    {
      "target": "程式",
      "variants": ["城市"]
    },
    {
      "target": "介面",
      "variants": ["接口"]
    },
    {
      "target": "影片",
      "variants": ["視頻"]
    },
    {
      "target": "軟體",
      "variants": ["軟件"]
    },
    {
      "target": "伺服器",
      "variants": ["服務器"]
    }
  ]
}
```

---

## 9.1 欄位定義

### `version`

詞庫格式版本。

目前：

```text
1
```

---

### `entries`

詞庫項目集合。

---

### `target`

使用者希望最終輸出的標準詞。

例如：

```text
程式
```

---

### `variants`

ASR 可能輸出的錯誤／近音／同音／不符合台灣習慣的詞。

例如：

```text
["城市"]
```

---

# 10. Custom Dictionary 處理

例如 ASR：

```text
我今天要修改城市的接口
```

OpenCC：

```text
我今天要修改城市的接口
```

Custom Dictionary：

```text
城市 → 程式
接口 → 介面
```

最終：

```text
我今天要修改程式的介面
```

再執行 Clipboard + Paste。

---

# 11. Dictionary 的 MVP 限制

MVP 只做：

* exact string matching
* deterministic replacement
* 一個 target 可以有多個 variants
* 多個 dictionary entries
* JSON validation

MVP 不做：

* AI 語意判斷
* 自動推測詞彙
* 模糊搜尋
* Embedding
* 拼音分析
* 語音重新辨識
* 自動學習

---

# 12. Model Selection

MVP 支援：

```text
Qwen3-ASR-0.6B
Qwen3-ASR-1.7B
```

使用者可以在兩者之間切換。

---

## 12.1 不使用主 GUI

不建立傳統設定視窗。

使用 OS 原生常駐選單。

### macOS

使用：

**Menu Bar**

例如：

```text
Voice Input

Model
  ✓ Qwen3-ASR-0.6B
    Qwen3-ASR-1.7B

Dictionary
  Open Dictionary

Status
  Ready

Quit
```

---

### Windows

使用：

**System Tray / Notification Area**

例如：

```text
Voice Input

Model
  ✓ Qwen3-ASR-0.6B
    Qwen3-ASR-1.7B

Dictionary
  Open Dictionary

Status
  Ready

Exit
```

---

# 13. Model 切換行為

使用者選擇另一個模型後：

```text
Current Model
 ↓
Unload / Release
 ↓
Load New Model
 ↓
Ready
```

不要求重新啟動程式。

如果模型尚未下載：

```text
Model Not Found
 ↓
Download / Initialize
 ↓
Load
 ↓
Ready
```

具體下載方式由 Coding LLM 根據 Qwen 官方推薦方案決定。

---

# 14. Model 設定持久化

模型選擇不得與 Custom Dictionary 綁定。

建議：

```text
config.json
custom_dictionary.json
```

分離。

例如：

```json
{
  "model": "0.6b"
}
```

Custom Dictionary 則維持獨立。

---

# 15. Clipboard / Paste

最終文字：

```text
Final Text
 ↓
Clipboard
 ↓
Paste Shortcut
```

平台：

### macOS

```text
Command + V
```

### Windows

```text
Ctrl + V
```

平台差異必須封裝在 platform layer。

核心文字處理不應知道：

* Cmd
* Ctrl
* macOS
* Windows

---

# 16. 錄音

MVP 必須：

* 使用系統預設麥克風。
* 開始錄音。
* 停止錄音。
* 將音訊交給 ASR。
* 支援 macOS。
* 支援 Windows。

不需要：

* 錄音檔管理
* WAV 匯出
* 音訊編輯
* 音量混音
* 多軌錄音

---

# 17. 系統狀態

至少有：

```text
Idle
Recording
Processing
Ready
Error
```

狀態轉換：

```text
Idle
 ↓ F8
Recording
 ↓ F8
Processing
 ↓
Ready
```

發生錯誤：

```text
任何狀態
 ↓
Error
 ↓
Ready / Idle
```

不得因為單次 ASR、錄音、詞庫或 Paste 錯誤導致整個程式無法繼續使用。

---

# 18. 錯誤處理

MVP 至少處理：

### Microphone

* 麥克風不存在
* 麥克風無法開啟
* 權限不足

### ASR

* 模型載入失敗
* 模型推論失敗
* 音訊格式錯誤
* 無辨識結果

### Dictionary

* JSON 不存在
* JSON 格式錯誤
* schema 不符合
* target / variants 欄位錯誤

### Clipboard / Paste

* Clipboard 操作失敗
* Paste 操作失敗

錯誤應提供簡單、可理解的提示。

---

# 19. 不懂程式使用者的要求

朋友不需要知道：

* Python
* pip
* virtualenv
* dependency
* ASR runtime
* Python package

正式發布版本應做到：

```text
下載
 ↓
啟動
 ↓
使用
```

Custom Dictionary 是唯一預期需要使用者直接修改的技術性檔案。

但 JSON 格式必須保持簡單、可讀。

---

# 20. MVP 不包含的功能

明確排除：

* 主 GUI
* 錄音檔管理
* 語音歷史
* 雲端同步
* 帳號
* 雲端 ASR
* 多語言 UI
* 自動更新
* AI Chat
* AI Rewrite
* 自動摘要
* 自動修正文法
* 自動學習
* Embedding
* Local LLM 語意校正
* Context-aware Dictionary

---

# 21. 後續版本：Context-aware Dictionary

MVP 的：

```text
ASR Variant → Target
```

會遇到一個核心問題：

> 同一個詞在不同上下文可能都是合理的。

例如：

```text
城市
```

可能真的代表：

```text
城市
```

也可能是：

```text
程式
```

因此第二階段增加 Context。

---

## 21.1 Context Dictionary

未來格式可以演進為：

```json
{
  "version": 2,
  "entries": [
    {
      "target": "程式",
      "variants": ["城市"],
      "context": [
        "寫",
        "修改",
        "開發",
        "執行",
        "編譯"
      ]
    }
  ]
}
```

例如：

```text
我正在寫城市
```

可能校正為：

```text
我正在寫程式
```

但：

```text
台北是一個城市
```

則不應自動替換。

---

# 22. Context-aware Dictionary 演進

### Level 1 — MVP

```text
Exact Variant
```

```text
城市 → 程式
```

---

### Level 2

```text
Context Rule
```

```text
寫 + 城市 → 程式
修改 + 城市 → 程式
編譯 + 城市 → 程式
```

---

### Level 3

語意層級判斷：

```text
整句文字
 ↓
Context Analysis
 ↓
判斷詞義
 ↓
選擇 Target
```

未來才考慮：

* NLP
* Embedding
* Local LLM
* Contextual Reranking

不要在 MVP 實作。

---

# 23. 建議專案架構

Coding LLM 可以依實際 implementation 調整，但應維持以下責任分離：

```text
src/
├── app/
│   ├── application.py
│   └── state.py
│
├── audio/
│   └── recorder.py
│
├── asr/
│   ├── interface.py
│   ├── qwen_asr.py
│   └── model_manager.py
│
├── text/
│   ├── traditional_chinese.py
│   └── dictionary.py
│
├── input/
│   ├── hotkey.py
│   ├── clipboard.py
│   └── paste.py
│
├── platform/
│   ├── macos.py
│   └── windows.py
│
├── settings/
│   ├── config.py
│   └── dictionary_loader.py
│
└── ui/
    └── tray.py
```

以上是**架構方向，不是強制檔案名稱**。

Coding LLM 可以根據實際套件與平台限制調整。

---

# 24. 重要架構原則

### ASR 不應知道 Clipboard

```text
ASR
```

只負責：

```text
Audio → Text
```

---

### Dictionary 不應知道 ASR

Dictionary 只負責：

```text
Text → Corrected Text
```

---

### Paste 不應知道中文

Paste 只負責：

```text
Text → Current Cursor
```

---

### Model Manager 不應知道 UI

Model Manager 只負責：

```text
Model Selection
Model Loading
Model Unloading
Model State
```

Menu Bar / Tray 只是呼叫它。

---

# 25. MVP 驗收測試

## 基本流程

* [ ] 啟動程式
* [ ] F8 開始錄音
* [ ] F8 停止錄音
* [ ] 成功取得 ASR 結果
* [ ] 結果為繁體中文
* [ ] 文字成功貼入目前游標

---

## Model

* [ ] 0.6B 可以正常使用
* [ ] 1.7B 可以正常使用
* [ ] 可以切換模型
* [ ] 切換後不需要重啟
* [ ] 模型設定可以保存
* [ ] 模型載入失敗有錯誤提示

---

## Traditional Chinese

* [ ] ASR 使用繁中導向設定
* [ ] OpenCC 正常運作
* [ ] 簡體輸出可以轉為繁體
* [ ] 台灣常用詞彙可由 Custom Dictionary 處理

---

## Custom Dictionary

測試：

```text
ASR：
城市

Dictionary：
程式 ← 城市

Result：
程式
```

以及：

```text
ASR：
接口

Dictionary：
介面 ← 接口

Result：
介面
```

確認：

* [ ] 一個 target 可以有多個 variants
* [ ] 多個 entries 可以同時運作
* [ ] 無詞庫項目時文字保持原樣
* [ ] JSON 格式錯誤不會造成程式 crash

---

## Cross Platform

### macOS

* [ ] F8 可用
* [ ] 麥克風可用
* [ ] ASR 可用
* [ ] Menu Bar 可用
* [ ] Cmd+V 可用
* [ ] 模型切換可用

### Windows

* [ ] F8 可用
* [ ] 麥克風可用
* [ ] ASR 可用
* [ ] System Tray 可用
* [ ] Ctrl+V 可用
* [ ] 模型切換可用

---

# 26. MVP 完成定義

MVP 只有在以下條件全部成立時才視為完成：

> **macOS 與 Windows 均可以啟動獨立版本。**

並且：

> **使用者可以按 F8 錄音，再按 F8 停止，使用 Qwen3-ASR-0.6B 或 1.7B 完成本地語音辨識。**

並且：

> **辨識結果經繁體中文處理與 Custom Dictionary 後，可以自動貼到目前游標位置。**

並且：

> **使用者可以透過簡單 JSON 詞庫自行處理同音／近音／台灣用語差異。**

並且：

> **使用者可以透過 macOS Menu Bar 或 Windows System Tray 在 0.6B / 1.7B 之間切換模型，而不需要主 GUI。**

---

# 27. 後續 Roadmap

```text
MVP
│
├─ F8 語音輸入
├─ Qwen3-ASR 0.6B / 1.7B
├─ 繁體中文
├─ OpenCC
├─ Custom Dictionary
├─ Clipboard + Paste
└─ macOS / Windows
       │
       ↓
Phase 2
│
├─ Context-aware Dictionary
├─ Context Rule
└─ 更精準的同音／近音詞判斷
       │
       ↓
Phase 3
│
├─ Semantic Context
├─ Local LLM / Embedding
└─ 智慧詞彙校正
```

**核心原則：先把「語音輸入」做到非常可靠，再增加「理解使用者想說什麼」的能力。**
